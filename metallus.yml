software_groups: &software
  - "base"

jobs:
  etcdctl:
    builder: "make"
    software_groups: *software
    base: "ubuntu:12.04"
    start_in: "etcd"
    packages:
      - type: debian
        name: etcdctl
        repos:
          - mxm-public
        target: install
